import React from 'react';
import ReactDOM from 'react-dom/client';
import Base from './01-base/Base';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Base />
  </React.StrictMode>
);
